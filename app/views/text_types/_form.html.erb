<%= form_for(@text_type) do |f| %>
  <% if @text_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@text_type.errors.count, "error") %> prohibited this text_type from being saved:</h2>

      <ul>
      <% @text_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
